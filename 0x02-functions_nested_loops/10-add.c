#include "main.h"

/**
 * add - check main
 * @a: an integer a
 * @b: an integer b
 * Description: function that adds two integers and returns the result
 * Return: Result to add to a and b
 */
int add(int a, int b)
{
	return (a + b);
}
