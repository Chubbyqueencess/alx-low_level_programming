#include "main.h"
/**
 * _abs - check Main
 * @r: An integer input
 * description: This function returns absolute value of a number
 * Return: Absoolute value of number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
